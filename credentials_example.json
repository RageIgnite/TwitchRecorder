{
  "client_id": "YOUR_TWITCH_CLIENT_ID",
  "client_secret": "YOUR_TWITCH_CLIENT_SECRET",
  "_comment": "This file is a fallback if client_id and client_secret are not specified in config.ini. The script will also prompt for credentials if they are not found in either file."
}